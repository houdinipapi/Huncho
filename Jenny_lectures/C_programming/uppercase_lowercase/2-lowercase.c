#include <stdio.h>
#include <string.h>

/**
 * main - converts letters into lowercase
 */

void main(void)
{
	int i, str_len;
	char str[30];

	printf("Enter a string in uppercase characters: ");
	gets(str);


